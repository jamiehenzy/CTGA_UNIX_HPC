# Tutorial One

## UNIX/LINUX command line interface (CLI)
For all commands, be familiar with typical options/flags, such as -l, .., -r, etc.

## Basic navigational commands: 
+ pwd
+ cd
+ ls

## Commands to create and interact with files and directories
+ mkdir
+ rm, rm -r
+ head, tail
+ cp
+ touch
+ echo
+ cat
+ less (spacebar, quit, how to search within file)
+ |
+ \>
+ \>>
+ cut
+ sort
+ uniq
+ tr
+ wc
+ sed
+ saving Terminal session as text file

## Searching and more
+ find
+ grep
+ use of wildcards, such as *, ., ^, $
+ wget (or curl)
+ chmod
+ ^C
+ clear
+ man
+ source
+ history
+ phred scores

## Simple scripts
+ bash script
+ for loop
+ variables
+ basename
+ string manipulation syntax (%)

## File formats
+ plain text
+ FASTA (.fasta, .fa, .fna, .faa)
+ FASTQ (.fastq, .fq)
+ general feature format (.gff)
+ comma-separated variable (.csv)

## HPC nodes: login, computing
+ login command:
+ command to request computing node:

## Directories on Explorer
+ /home/<username>
+ /scratch/<username>
+ /courses/BIOL3411.202540/student/<username>

## Best practices
+ README files
+ ab-complete
+ naming files: legal characters; optimized for tab-complete
+ organizing material
+ #(hash) commenting
+ Saving screen output
+ editing extraneous text from saved screen output
+ Do not use HPC login node for anything that requires memory, computation

## Troubleshooting
+ Cut and paste error into Google, chat, Claude
+ Bioinfo forums: indicate command/code used and output
+ Discussion forum; in-class session

